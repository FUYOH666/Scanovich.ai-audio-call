# 📊 Руководство по аналитике ошибок

**Author:** Aleksandr Mordvinov  
**Project:** ASR-4.5 Analytics Module

---

## 🎯 Концепция: Error-Centric подход

**Собираем ТОЛЬКО ошибки** (failed/unknown критерии) → Агрегируем → Показываем руководителям с примерами.

**Преимущества:**
- ✅ Фокус на проблемах (не тонем в данных)
- ✅ Конкретные примеры для обучения
- ✅ Быстрые запросы (SQLite с индексами)
- ✅ Автоматические отчёты (Telegram)

---

## 📂 Структура данных

### SQLite БД (`analytics/analytics.db`):

**Таблица error_events:**
- Все случаи невыполнения критериев (score < 1.0)
- Severity: required (1-20) или optional (21-30)
- Status: failed (не выполнено) или unknown (неприменимо)
- Quote: цитата из транскрипции

**Таблица calls_summary:**
- Сводка по каждому звонку
- ERR: есть ли ошибки
- Количество ошибок (required/optional)

---

## 📊 Ключевые метрики

### 1. ERR (Error Rate)
**Доля звонков с ≥1 ошибкой**

Формула: `calls_with_errors / total_calls`

Интерпретация:
- ERR < 70% - хорошо ✅
- ERR 70-85% - требует внимания ⚠️
- ERR > 85% - критично ❌

### 2. MissRate (по параметру)
**Доля невыполнений конкретного критерия**

Формула: `miss_count / total_calls`

Интерпретация:
- MissRate < 50% - приемлемо
- MissRate 50-80% - системная проблема
- MissRate > 80% - критично (скрипт не работает!)

### 3. Unknown-share
**Доля "неизвестных" статусов**

Если >7% - проблемы с качеством звука или скриптом

---

## 🛠️ CLI Команды

### Агрегация витрин:
```bash
./venv/bin/python main.py aggregate --period day
./venv/bin/python main.py aggregate --period week
```

### Отправка Telegram отчётов:
```bash
./venv/bin/python main.py telegram-report --type daily
./venv/bin/python main.py telegram-report --type weekly
```

### Экспорт CSV:
```bash
./venv/bin/python main.py export-csv --period week --output report.csv
./venv/bin/python main.py export-csv --admin "Дарья"
```

### Статистика БД:
```bash
./venv/bin/python main.py error-stats
```

---

## 📱 Настройка Telegram

### 1. Получить chat_id:

```bash
# Отправьте боту @i18_autogen_bot сообщение /start
# Затем запустите:
./venv/bin/python << 'EOF'
import asyncio
from telegram import Bot

bot = Bot(token="7809858128:AAE8im5Og3Ma-tsPw60fudLAL_sC7Lrx7lA")

async def get_chat_id():
    updates = await bot.get_updates()
    for u in updates[-5:]:
        if u.message:
            print(f"Chat ID: {u.message.chat_id}")

asyncio.run(get_chat_id())
EOF
```

### 2. Обновить config.yaml:

```yaml
analytics:
  telegram:
    enabled: true
    chat_id: "YOUR_CHAT_ID"  # Скопируйте из предыдущей команды
```

### 3. Тестовая отправка:

```bash
./venv/bin/python main.py telegram-report --type daily
```

---

## 📈 Примеры отчётов

### Ежедневный отчёт (директору):

```
📊 Ежедневный отчёт | 20.10.2025

Звонков сегодня: 32
ERR: 78% (25 звонков с ошибками)

⚠️ Top-3 провала (required):
1. Видеозаключение - 27 звонков (84%)
2. Льготы/скидки - 23 звонка (72%)
3. Длительность симптомов - 22 звонка (69%)

👤 Администраторы:
• Дарья - ERR 95% (19/20 звонков) ❌
• Алёна - ERR 70% (7/10) ⚠️
• Наталья - ERR 50% (1/2) ✅
```

### Еженедельный отчёт (владельцу):

```
📊 Недельный отчёт | 14-20.10

Всего звонков: 1,344
Средний ERR: 74%

🏆 Рейтинг администраторов:
1. Наталья - ERR 52% ✅ (48 звонков)
2. Алёна - ERR 68% ⚠️ (96)
3. Дарья - ERR 91% ❌ (120)
```

---

## 💡 Интерпретация результатов

### Top-3 провала (из реальных данных):

1. **"Вопрос о длительности симптомов" - 100% (15/15)**
   - Проблема: Никто не спрашивает!
   - Действие: Добавить в чек-лист, обучить всех
   - Приоритет: КРИТИЧНО

2. **"Вопрос о характере боли" - 87% (13/15)**
   - Проблема: Забывают уточнить
   - Действие: Тренинг + напоминание в скрипте

3. **"Описание видеозаключения" - 87% (13/15)**
   - Проблема: Не объясняют что это и зачем
   - Действие: Обновить скрипт с шаблоном объяснения

---

## 🎓 Как использовать для обучения

### Workflow:

1. **Ежедневный мониторинг** (09:00 Telegram отчёт)
   - Смотрим ERR по администраторам
   - Если ERR > 85% → срочный разговор

2. **Еженедельный анализ** (понедельник)
   - Смотрим Top-3 провала
   - Выбираем 1-2 критерия для улучшения на неделю

3. **Экспорт примеров** (для тренинга)
   ```bash
   ./venv/bin/python main.py export-csv --admin "Дарья" --output darya_errors.csv
   ```
   - Открываем в Excel
   - Находим конкретные цитаты
   - Используем на тренинге

4. **Мониторинг улучшения**
   - Смотрим динамику (week over week)
   - Если MissRate падает → обучение работает! ✅

---

## ⚙️ Автоматизация (в daemon)

При включении `analytics.enabled: true`:

1. После каждого анализа качества → автоматически извлекает ошибки → SQLite
2. Каждый час → агрегирует витрины
3. Каждый день 09:00 → отправляет daily отчёт в Telegram
4. Каждый понедельник 10:00 → weekly отчёт

**Никакого ручного труда!**

---

## 📋 SLO (Service Level Objectives)

- ✅ **Свежесть витрин:** ≤2 мин (обновляются каждый час)
- ✅ **Валидность JSON:** 100%
- ✅ **Unknown-share:** <7% (иначе проблемы с качеством звука)
- ✅ **Покрытие:** 100% звонков попадают в аналитику

---

**© 2025 ScanovichAI | Aleksandr Mordvinov**

